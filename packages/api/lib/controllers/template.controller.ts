import { Request, Response } from 'express'
/**
 * Template controller which can be used to quickly create a new ones.
 */
export class TemplateController {
	constructor() {}
	public templateFunction(req: Request, res: Response) {}
}
