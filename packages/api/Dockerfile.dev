FROM node:12

ENV NODE_ENV=development

WORKDIR /api

COPY package.json package.json ./

RUN yarn install

# Prisma initialization
RUN npm install -g prisma
RUN prisma generate

EXPOSE 3600

CMD ["yarn", "run", "dev"]
